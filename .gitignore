.env
__pycache__/
*.pyc
.venv/
venv/

# The music library itself is not source. Keep the mood folders, drop the audio.
songs/**/*.mp3
songs/**/*.ogg
songs/**/*.wav
songs/**/*.m4a
songs/**/*.flac
songs/**/*.opus
!songs/**/.gitkeep

# Runtime state: track ratings. Yours, not the project's.
data/

# Local overrides of the button layout
deck.local.yaml
